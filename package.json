{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "concurrently  \"node --env-file=.env --watch-path=./ --watch server.js\" \"npm run dev --prefix frontend\"",
    "frontend": "npm install --prefix frontend",
    "install:all": "npm install && npm run frontend ",
    "build": "npm run build --prefix frontend",
    "start": "node server.js",
    "seed": "node --env-file=.env seeds/seed.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "joi": "^18.0.2",
    "morgan": "^1.10.1",
    "mysql2": "^3.15.3",
    "pg": "^8.16.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.7"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.6.2"
  }
}
